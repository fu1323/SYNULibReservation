#某学校（如沈阳师范大学）图书馆座位预约系统的自动化预约实现（利用了联创服务器用客户端302传unionid给学校服务器的问题,本程序绕过了密码登陆,直接用unionid换token） 针对杭州联创的系统  <br>
##使用说明<br>
请自行编译，java -jar xxx.jar 可看到效果，初次运行会生成json配置文件，抓包填写即可 <br>
renew字段默认false，改成true可自动续期（仅macos、LINUX,自动续期对windows无效 因为使用了at命令 windows逻辑请自行处理）<br>
座位可填写多个不限制，当程序运行发现座位被占用会依次尝试<br>
unionid 座位id(devSn)可通过抓包获得，自己微信扫桌子上的二维码的时候抓包即可<br>
续期通常自动执行(实际是java -jar xxx.jar renew，开启续期会自动生成renew.json,同时插入at自动任务 <br>图书馆闭馆时间是晚上十点，而单次最大预约市场五小时，所以若在下午四点之后 程序执行将不会再排今天的续期定时任务)<br>
程序自动续期只对一天有效，如果想每天自动执行 无感预约 请配合crontab使用<br>
<br><br>
更新了座位号和devsn对应的json文件，可以直接去查


26.9-1更新: <br>
1、座位预约支持优先级（id后面的数字，越小越优先）排序 可不限数量  当座位被占用时会根据优先级自动顺延尝试<br>
2、本次修复了当优先级序号出现跳过时程序会出现非预期行为的问题，配置文件格式进行了更新，(旧版本程序生成配置文件，新版无法兼容，变化不大 但请重新整理)支持备注字段，可以备注座位号 方便人工阅读 修改<br>
3、支持续期fallback,即续期因座位被别人预约而失败时 支持自动重新从心仪座位挑选 预约(配置谇)<br>
26.9-2更新: <br>
更新了当所有心仪座位均没有预约到的时候 程序支持15分钟后自动再次尝试，（at命令调度) 最多重复尝试三次<br>
更新了 重试部分的开启 间隔 次数 支持配置文件控制   注意:配置文件需要更新，添加了此部分的几个字段，可以让程序在没有配置文件的目录运行一次会自动生成配置文件模版，否则沿用旧配置文件会空指针<br>

p.s. 工具逻辑正常  但是代码被改成屎山了  毕竟自己当初也没想到会有这么多种情况 逻辑   有需要的 如果自己有能力 辛苦重构一下 再放出来  学长快毕业了
